<nav id="nav">
  <div class="nav-logo" role="link" tabindex="0" onclick="window.scrollTo({top:0,behavior:&#39;smooth&#39;})">Get<span class="logo-mine">Mine.</span><span class="logo-ai">ai</span></div>
  <div class="nav-links">
    <a onclick="scrollToSection(&#39;why-it-matters&#39;)" style="color:#1DDEC8">Why it matters</a>
    <a onclick="scrollToSection(&#39;our-promise&#39;)">Our promise</a>
    <a onclick="scrollToSection(&#39;why-getmine&#39;)">What becomes possible</a>
    <a onclick="scrollToSection(&#39;early-access-preview&#39;)">Early access</a>
    <a onclick="scrollToSection(&#39;about&#39;)">About us</a>
    <button class="nav-cta" onclick="openWaitlist()" aria-controls="waitlist mobile-waitlist" aria-expanded="false">Join Early Access</button>
  </div>
  <button class="nav-hamburger" onclick="openMobileMenu()" aria-label="Open menu" aria-controls="mobile-menu" aria-expanded="false">
    <span></span><span></span><span></span>
  </button>
</nav>

<!-- ── MOBILE MENU DRAWER ─────────────────────────────────────────
  Full-screen overlay shown when the hamburger is tapped on mobile.
  Only the sections that exist on mobile (i.e. not "Why it matters" or
  "What becomes possible", which are hidden ≤880px) appear here. -->
<div id="mobile-menu" aria-hidden="true" role="dialog" aria-label="Site menu">
  <header class="mm-nav">
    <div class="nav-logo" onclick="closeMobileMenu();window.scrollTo({top:0,behavior:'smooth'})">Get<span class="logo-mine">Mine.</span><span class="logo-ai">ai</span></div>
    <button class="mm-close" onclick="closeMobileMenu()" aria-label="Close menu">×</button>
  </header>
  <nav class="mm-body">
    <ul class="mm-links">
      <li><a onclick="closeMobileMenu();scrollToSection('our-promise')">Our promise</a></li>
      <li><a onclick="closeMobileMenu();scrollToSection('early-access-preview')">Early access</a></li>
      <li><a onclick="closeMobileMenu();scrollToSection('about')">About us</a></li>
    </ul>
    <div class="mm-actions">
      <button class="mm-btn mm-btn-primary" onclick="closeMobileMenu();openWaitlist()" aria-controls="waitlist mobile-waitlist" aria-expanded="false">Join Early Access →</button>
    </div>
  </nav>
</div>
