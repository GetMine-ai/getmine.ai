<section class="download-cta section-edge" aria-labelledby="download-cta-title">
  <div class="wrap">
    <p class="eyebrow">Open beta · Free to download</p>
    <h2 id="download-cta-title">Your health. <em>Your data.</em> <span class="rules">Your rules.</span></h2>
    <p>Start your vault on your own computer, with Mina included.</p>
    <a class="primary-cta" href="/beta">Download GetMine</a>
  </div>
</section>

<style>
  .download-cta {
    background: var(--navy);
    text-align: center;
  }

  .download-cta .wrap {
    display: grid;
    justify-items: center;
    padding-block: clamp(4rem, 8vw, 7rem);
  }

  h2 {
    max-width: 52rem;
    margin: 1rem 0 0;
    color: var(--cream);
    font-family: var(--font-display);
    font-size: var(--text-h2);
    font-weight: 300;
    line-height: 1.08;
    text-wrap: balance;
  }

  h2 em {
    color: var(--teal);
    font-style: normal;
  }

  .download-cta #download-cta-title .rules {
    color: var(--taupe);
  }

  p:not(.eyebrow) {
    max-width: 34rem;
    margin: 1.25rem 0 0;
    color: var(--cream);
    font-size: var(--text-lead);
    line-height: 1.6;
  }

  .primary-cta {
    margin-top: 2rem;
  }
</style>
