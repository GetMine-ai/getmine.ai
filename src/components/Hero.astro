<section class="hero" id="top">
  <div class="hero-light" aria-hidden="true"></div>
  <div class="wrap-wide hero-grid">
    <div class="hero-copy">
      <h1>
        <span>Your health.</span>
        <span>Your data.</span>
        <span>Your rules.</span>
      </h1>
      <p>
        Your health data, brought together in one private vault. Mina, your personal
        health agent, helps you put it to work.
      </p>
      <div class="hero-actions" id="hero-cta">
        <a class="primary-cta" href="/beta">Request beta access</a>
        <span>Open beta · Rolling out in waves</span>
      </div>
    </div>

    <div class="product-scene">
      <svg class="memory-line" viewBox="0 0 1000 250" aria-hidden="true">
        <path d="M0 145 C100 80 160 220 255 145 S415 70 500 145 S670 220 760 145 S915 70 1000 145" />
        <circle cx="255" cy="145" r="5" />
        <circle cx="500" cy="145" r="5" />
        <circle cx="760" cy="145" r="5" />
      </svg>
      <picture>
        <source srcset="/assets/redesign/getmine-desk-scene.avif" type="image/avif" />
        <source srcset="/assets/redesign/getmine-desk-scene.webp" type="image/webp" />
        <img
          src="/assets/redesign/getmine-desk-scene.png"
          alt="GetMine running on a desktop monitor showing Your Vault, with keyboard and mouse on a desk"
          width="954"
          height="828"
          loading="eager"
          fetchpriority="high"
        />
      </picture>
      <div class="scene-shade" aria-hidden="true"></div>
      <p>GetMine for macOS and Windows</p>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    min-height: min(57rem, 100svh);
    padding-top: 4.75rem;
    background: var(--navy);
  }

  .hero-light {
    position: absolute;
    z-index: 0;
    top: 18%;
    right: -10%;
    width: 42rem;
    height: 42rem;
    border-radius: 50%;
    background: radial-gradient(closest-side, var(--teal), transparent 72%);
    filter: blur(9rem);
    opacity: 0.16;
    pointer-events: none;
  }

  .hero-grid {
    position: relative;
    z-index: 1;
    display: grid;
    align-items: center;
    gap: 3rem;
    padding-block: 2.5rem 3.5rem;
  }

  .hero-copy {
    max-width: 35rem;
  }

  h1 {
    margin: 0;
    font-family: var(--font-display);
    font-size: var(--text-hero);
    font-weight: 300;
    letter-spacing: -0.05em;
    line-height: 0.94;
  }

  h1 span {
    display: block;
  }

  h1 span:nth-child(2) {
    color: var(--teal);
  }

  h1 span:nth-child(3) {
    color: var(--taupe);
  }

  .hero-copy > p {
    max-width: 33.75rem;
    margin: 1.75rem 0 0;
    color: var(--text-secondary);
    font-size: var(--text-lead);
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1rem;
    margin-top: 2.25rem;
  }

  .hero-actions > span {
    color: var(--text-quiet);
    font-size: 0.9375rem;
    line-height: 1.5;
  }

  .product-scene {
    position: relative;
    min-width: 0;
  }

  .product-scene picture {
    position: relative;
    z-index: 1;
  }

  .product-scene img {
    width: 100%;
    height: auto;
    user-select: none;
    mask-image: radial-gradient(140% 120% at 55% 45%, #000 50%, transparent 95%);
  }

  .memory-line {
    position: absolute;
    z-index: 0;
    inset: 8% -12% auto;
    width: 124%;
    height: 70%;
    overflow: visible;
    opacity: 0.16;
  }

  .memory-line path {
    fill: none;
    stroke: var(--teal);
    stroke-width: 2;
  }

  .memory-line circle {
    fill: var(--teal);
  }

  .scene-shade {
    position: absolute;
    z-index: 2;
    inset: 0;
    background: radial-gradient(
      120% 100% at 55% 45%,
      transparent 20%,
      rgb(6 12 20 / 60%) 65%,
      rgb(6 12 20 / 95%) 100%
    );
    mix-blend-mode: multiply;
    pointer-events: none;
  }

  .product-scene > p {
    position: relative;
    z-index: 3;
    margin: 0.75rem 0 0;
    color: rgb(199 194 182 / 80%);
    font-size: 0.9375rem;
    font-weight: 500;
    letter-spacing: 0.025em;
    text-align: center;
  }

  @media (min-width: 64rem) {
    .hero-grid {
      grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
      gap: 3.5rem;
      padding-block: 3.5rem 5rem;
    }

    .product-scene {
      margin-right: -2.5rem;
    }
  }

  @media (min-width: 40rem) {
    .hero-copy > p {
      font-size: 1.1875rem;
    }
  }
</style>
