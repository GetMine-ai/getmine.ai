<section class="hero" id="top">
  <svg class="memory-line" viewBox="0 0 1600 520" aria-hidden="true">
    <path d="M0 306 C165 190 255 422 410 306 S700 190 845 306 S1140 422 1290 306 S1500 190 1600 306" />
    <circle cx="410" cy="306" r="5" />
    <circle cx="845" cy="306" r="5" />
    <circle cx="1290" cy="306" r="5" />
  </svg>

  <div class="hero-grid">
    <div class="hero-copy">
      <span class="hero-badge">
        <span aria-hidden="true"></span>
        OPEN BETA · FREE TO DOWNLOAD
      </span>

      <h1>
        <span>Your health.</span>
        <span>Your data.</span>
        <span>Your rules.</span>
      </h1>

      <p class="hero-intro">
        Your health records, <span class="hero-intro-accent">finally together</span> in one
        private vault on your computer. Mina, your personal health agent, helps make
        sense of it all.
      </p>

      <a class="primary-cta hero-download" href="/beta">Download GetMine</a>

      <div class="privacy-note">
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M7.5 10V7.5a4.5 4.5 0 0 1 9 0V10"></path>
          <rect x="5.5" y="10" width="13" height="11" rx="2"></rect>
          <path d="M12 14.5v2.5"></path>
        </svg>
        <p>
          <strong>Free for macOS and Windows</strong>
          <span>Your vault stays on your computer. We never hold a central copy.</span>
        </p>
      </div>

      <div class="hero-roadmap">
        <p class="roadmap-label">Coming next</p>
        <ul>
          <li>
            <span class="roadmap-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none"><path d="M8 11a3 3 0 1 0 0-6 3 3 0 0 0 0 6zM16 11a3 3 0 1 0 0-6 3 3 0 0 0 0 6zM3 21c0-4 2-7 5-7s5 3 5 7M11 21c0-4 2-7 5-7s5 3 5 7" /></svg>
            </span>
            Family Vault
          </li>
          <li>
            <span class="roadmap-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none"><circle cx="18" cy="5.5" r="2.5" /><circle cx="6" cy="12" r="2.5" /><circle cx="18" cy="18.5" r="2.5" /><path d="M8.2 10.8l7.6-4M8.2 13.2l7.6 4" /></svg>
            </span>
            Secure sharing
          </li>
          <li>
            <span class="roadmap-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none"><rect x="7.5" y="6.5" width="9" height="11" rx="2.5" /><path d="M10 6.5V3.5h4v3M10 17.5v3h4v-3M18 10.75v2.5" /></svg>
            </span>
            Wearables
          </li>
          <li>
            <span class="roadmap-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none"><rect x="5.5" y="3" width="13" height="18" rx="2.5" /><path d="M12 9.5v5M9.5 12h5" /></svg>
            </span>
            Health passport
          </li>
        </ul>
      </div>
    </div>

    <div class="product-visual">
      <div class="artwork-window">
        <img
          src="/assets/redesign/getmine-hero-approved.png"
          alt="GetMine desktop health vault showing Ask Mina, Health Story and connected health sources."
          width="1448"
          height="1086"
          loading="eager"
          fetchpriority="high"
        />
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: clamp(51.25rem, 100svh, 56.25rem);
    overflow: hidden;
    padding-top: 5.25rem;
    background: var(--navy);
  }

  .hero-grid {
    position: relative;
    z-index: 1;
    display: grid;
    width: min(100%, 96rem);
    min-height: calc(clamp(51.25rem, 100svh, 56.25rem) - 5.25rem);
    margin-inline: auto;
    align-items: center;
    gap: clamp(1.5rem, 3vw, 3.25rem);
    /* Trimmed ~2rem total when the roadmap row joined the hero (design review
       P0-R/R3) so the whole hero still fits a 900px-tall desktop viewport. */
    padding: clamp(1.5rem, 3vh, 3rem) clamp(1.25rem, 3.5vw, 3.25rem)
      clamp(1.5rem, 2.5vh, 2.5rem);
  }

  .hero-copy {
    position: relative;
    z-index: 2;
    max-width: 35rem;
  }

  .hero-badge {
    display: inline-flex;
    min-height: 3.125rem;
    align-items: center;
    gap: 0.75rem;
    border: 1px solid var(--teal);
    border-radius: 999px;
    padding: 0.65rem 1.15rem;
    color: var(--teal);
    font-family: var(--font-ui);
    font-size: 1.0625rem;
    font-weight: 700;
    letter-spacing: 0.075em;
    line-height: 1;
    animation: hero-badge-pulse 3.2s ease-in-out infinite;
  }

  .hero-badge > span {
    width: 0.75rem;
    height: 0.75rem;
    flex: 0 0 auto;
    border-radius: 50%;
    background: var(--teal);
    box-shadow: 0 0 0.7rem rgb(55 207 195 / 28%);
  }

  h1 {
    margin: clamp(1.75rem, 3.8vh, 2.75rem) 0 0;
    color: var(--cream);
    font-family: var(--font-display);
    font-size: clamp(4.75rem, 5.8vw, 5.625rem);
    font-weight: 300;
    letter-spacing: -0.055em;
    line-height: 1;
  }

  h1 span {
    display: block;
    white-space: nowrap;
  }

  h1 span:nth-child(2) {
    color: var(--teal);
  }

  h1 span:nth-child(3) {
    color: var(--taupe);
  }

  .hero-intro {
    max-width: 34rem;
    margin: clamp(1.75rem, 3.8vh, 2.5rem) 0 0;
    color: var(--cream);
    font-size: clamp(1.125rem, 1.4vw, 1.3125rem);
    line-height: 1.55;
  }

  .hero-download {
    min-width: 20rem;
    min-height: 4rem;
    margin-top: clamp(1.75rem, 3.5vh, 2.5rem);
    padding-inline: 2.25rem;
  }

  .hero-intro-accent {
    color: var(--teal);
  }

  .privacy-note {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    margin-top: 1.5rem;
    color: var(--cream);
  }

  .privacy-note svg {
    width: 2rem;
    height: 2rem;
    flex: 0 0 auto;
    fill: none;
    stroke: var(--teal);
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.5;
  }

  .privacy-note p {
    margin: 0;
    font-size: 1.0625rem;
    line-height: 1.45;
  }

  .privacy-note strong,
  .privacy-note span {
    display: block;
  }

  .privacy-note strong {
    font-weight: 600;
  }

  .privacy-note span {
    opacity: 0.82;
  }

  .product-visual {
    position: relative;
    min-width: 0;
  }

  .artwork-window {
    position: relative;
    width: 100%;
    overflow: hidden;
    aspect-ratio: 1.096;
  }

  .artwork-window img {
    position: absolute;
    top: 0;
    left: 0;
    width: 163.1%;
    max-width: none;
    height: auto;
    transform: translate(-38.7%, -12%);
    user-select: none;
  }

  .memory-line {
    position: absolute;
    z-index: 0;
    top: 24%;
    left: 50%;
    width: max(100%, 100rem);
    height: 48%;
    transform: translateX(-50%);
    overflow: visible;
    opacity: 0.12;
    pointer-events: none;
  }

  .memory-line path {
    fill: none;
    stroke: var(--teal);
    stroke-width: 1.5;
  }

  .memory-line circle {
    fill: var(--teal);
  }

  @keyframes hero-badge-pulse {
    0%,
    100% {
      box-shadow: 0 0 0 rgb(55 207 195 / 0%);
    }
    50% {
      box-shadow: 0 0 1rem rgb(55 207 195 / 8%);
    }
  }

  @media (min-width: 56rem) {
    .hero-grid {
      grid-template-columns: minmax(23rem, 0.78fr) minmax(34rem, 1.22fr);
    }
  }

  @media (max-width: 74rem) and (min-width: 56rem) {
    .hero-grid {
      grid-template-columns: minmax(20rem, 0.85fr) minmax(28rem, 1.15fr);
      gap: 1.25rem;
    }

    h1 {
      font-size: clamp(3.625rem, 6vw, 4.875rem);
    }

    .hero-intro {
      font-size: 1.125rem;
    }

    .hero-download {
      min-width: 17rem;
    }
  }

  @media (max-width: 55.9375rem) {
    .hero {
      min-height: auto;
    }

    .hero-grid {
      min-height: auto;
      gap: 2.25rem;
      padding-top: 2.25rem;
      padding-bottom: 3.5rem;
    }

    .hero-copy {
      max-width: none;
    }

    .hero-badge {
      min-height: 2.875rem;
      font-size: 0.9375rem;
      letter-spacing: 0.055em;
    }

    h1 {
      margin-top: 1.75rem;
      font-size: clamp(3rem, 14vw, 3.5rem);
      line-height: 1.02;
    }

    .hero-intro {
      margin-top: 1.5rem;
      font-size: 1.125rem;
    }

    .hero-download {
      width: min(100%, 25rem);
      min-width: 0;
      min-height: 3.75rem;
      margin-top: 1.75rem;
    }

    .platform-note,
    .privacy-note p {
      font-size: 1rem;
    }

    .artwork-window {
      width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-badge {
      animation: none;
    }
  }

  .hero-roadmap {
    margin-top: 1.25rem;
  }

  .roadmap-label {
    margin: 0;
    color: var(--teal);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  /* two items, one small row — decluttered to Family Vault + Secure sharing
     (ruled 4 Aug) */
  .hero-roadmap ul {
    display: grid;
    max-width: 23rem;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.75rem 0.625rem;
    margin: 0.75rem 0 0;
    padding: 0;
    list-style: none;
  }

  .hero-roadmap li {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: var(--cream);
    font-size: 0.875rem;
    font-weight: 500;
    line-height: 1.25;
    text-align: center;
  }

  .roadmap-icon {
    display: grid;
    width: 2.75rem;
    height: 2.75rem;
    border-radius: 50%;
    background: var(--teal);
    place-items: center;
  }

  .roadmap-icon svg {
    width: 1.25rem;
    height: 1.25rem;
  }

  .roadmap-icon path,
  .roadmap-icon rect,
  .roadmap-icon circle {
    stroke: #fff;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
  }

  @media (min-width: 64rem) {
    .hero-roadmap ul {
      /* wide enough for one-line labels at the desktop font size */
      max-width: 30rem;
    }

    .hero-roadmap li {
      font-size: 0.9375rem;
    }
  }
</style>
