<section class="hero" id="top">
  <div class="hero-light" aria-hidden="true"></div>
  <div class="wrap-wide hero-grid">
    <div class="hero-copy">
      <span class="beta-status">Open beta · Rolling out in waves</span>
      <h1>
        <span>Your health.</span>
        <span>Your data.</span>
        <span>Your rules.</span>
      </h1>
      <p>
        Your doctors’ records, hospital letters, health emails and your own notes.
        All in one vault on your own computer. Ask Mina, your personal health agent,
        anything about it.
      </p>
      <div class="hero-actions" id="hero-cta">
        <a class="primary-cta" href="/beta">Request beta access</a>
      </div>
    </div>

    <div class="product-scene">
      <svg class="memory-line" viewBox="0 0 1000 250" aria-hidden="true">
        <path d="M0 145 C100 80 160 220 255 145 S415 70 500 145 S670 220 760 145 S915 70 1000 145" />
        <circle cx="255" cy="145" r="5" />
        <circle cx="500" cy="145" r="5" />
        <circle cx="760" cy="145" r="5" />
      </svg>
      <picture>
        <source srcset="/assets/redesign/getmine-desk-scene.avif" type="image/avif" />
        <source srcset="/assets/redesign/getmine-desk-scene.webp" type="image/webp" />
        <img
          src="/assets/redesign/getmine-desk-scene.png"
          alt="GetMine running on a desktop monitor showing Your Vault, with keyboard and mouse on a desk"
          width="954"
          height="828"
          loading="eager"
          fetchpriority="high"
        />
      </picture>
      <div class="scene-shade" aria-hidden="true"></div>
      <p>GetMine for macOS and Windows</p>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    min-height: min(57rem, 100svh);
    padding-top: 4.75rem;
    background: var(--navy);
  }

  .hero-light {
    position: absolute;
    z-index: 0;
    top: 18%;
    right: -10%;
    width: 42rem;
    height: 42rem;
    border-radius: 50%;
    background: radial-gradient(closest-side, var(--teal), transparent 72%);
    filter: blur(9rem);
    opacity: 0.16;
    pointer-events: none;
  }

  .hero-grid {
    position: relative;
    z-index: 1;
    display: grid;
    align-items: center;
    gap: 3rem;
    padding-block: 2.5rem 3.5rem;
  }

  .hero-copy {
    max-width: 39rem;
  }

  h1 {
    margin: 1.25rem 0 0;
    font-family: var(--font-display);
    font-size: var(--text-hero);
    font-weight: 300;
    letter-spacing: -0.065em;
    line-height: 0.93;
  }

  h1 span {
    display: block;
  }

  h1 span:nth-child(2) {
    color: var(--teal);
  }

  h1 span:nth-child(3) {
    color: var(--taupe);
  }

  .hero-copy > p {
    max-width: 33.75rem;
    margin: 1.75rem 0 0;
    color: var(--text-secondary);
    font-size: var(--text-lead);
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1rem;
    margin-top: 2.85rem;
  }

  .beta-status {
    display: inline-flex;
    align-items: center;
    min-height: 2.75rem;
    padding: 0.625rem 1.125rem;
    border: 1px solid var(--teal-line);
    border-radius: var(--radius-pill);
    animation: beta-flash 2.4s var(--ease-gm) infinite;
    background: rgb(29 191 176 / 14%);
    color: var(--teal);
    font-size: clamp(1rem, 1.8vw, 1.25rem);
    font-weight: 700;
    letter-spacing: 0.025em;
    line-height: 1.3;
  }

  .product-scene {
    position: relative;
    min-width: 0;
  }

  .product-scene picture {
    position: relative;
    z-index: 1;
  }

  .product-scene img {
    width: 100%;
    height: auto;
    user-select: none;
    mask-image: radial-gradient(140% 120% at 55% 45%, #000 50%, transparent 95%);
  }

  .memory-line {
    position: absolute;
    z-index: 0;
    inset: 8% -12% auto;
    width: 124%;
    height: 70%;
    overflow: visible;
    opacity: 0.16;
  }

  .memory-line path {
    fill: none;
    stroke: var(--teal);
    stroke-width: 2;
  }

  .memory-line circle {
    fill: var(--teal);
  }

  .scene-shade {
    position: absolute;
    z-index: 2;
    inset: 0;
    background: radial-gradient(
      120% 100% at 55% 45%,
      transparent 20%,
      rgb(6 12 20 / 60%) 65%,
      rgb(6 12 20 / 95%) 100%
    );
    mix-blend-mode: multiply;
    pointer-events: none;
  }

  .product-scene > p {
    position: relative;
    z-index: 3;
    margin: 0.75rem 0 0;
    color: var(--cream);
    font-size: 0.9375rem;
    font-weight: 500;
    letter-spacing: 0.025em;
    text-align: center;
  }

  @media (min-width: 64rem) {
    .hero-grid {
      grid-template-columns: minmax(0, 1.02fr) minmax(0, 0.98fr);
      gap: 3.5rem;
      min-height: calc(min(57rem, 100svh) - 4.75rem);
      padding-block: 3.75rem 3rem;
    }

    .product-scene {
      margin-right: -3.5rem;
      transform: scale(1.055);
      transform-origin: center right;
    }
  }

  @media (min-width: 40rem) {
    .hero-copy > p {
      font-size: 1.1875rem;
    }
  }

  @keyframes beta-flash {
    0%, 100% {
      border-color: var(--teal-line);
      background: rgb(29 191 176 / 10%);
      box-shadow:
        0 0 0 0 rgb(29 191 176 / 0%),
        0 0 0.75rem rgb(29 191 176 / 8%);
      filter: brightness(1);
    }
    50% {
      border-color: var(--teal);
      background: rgb(29 191 176 / 22%);
      box-shadow:
        0 0 0 0.125rem rgb(29 191 176 / 18%),
        0 0 2rem 0 rgb(29 191 176 / 44%);
      filter: brightness(1.18);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .beta-status { animation: none; }
  }
</style>
