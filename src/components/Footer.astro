<footer>
  <div class="wrap footer-main">
    <div>
      <a class="wordmark" href="/#top" aria-label="GetMine.ai — home">
        Get<span>Mine.</span><i>ai</i>
      </a>
      <p class="tagline">
        <span>Your health.</span>
        <b>Your data.</b>
        <i>Your rules.</i>
      </p>
    </div>

    <nav aria-label="Footer">
      <a href="/privacy">Privacy</a>
      <a href="/terms">Terms</a>
      <a href="mailto:hello@getmine.ai">Contact</a>
    </nav>
  </div>

  <div class="wrap footer-meta">
    <p>GetMine is a local-first personal health vault for macOS and Windows.</p>
    <p>© 2026 GetMine</p>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--line-quiet);
    background: var(--navy);
    color: var(--cream);
  }

  .footer-main {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: start;
    gap: var(--space-6);
    padding-block: var(--space-12);
  }

  .wordmark {
    display: inline-flex;
    align-items: baseline;
    color: var(--cream);
    font-family: var(--font-display);
    font-size: 25px;
    font-weight: 300;
    letter-spacing: -0.035em;
    text-decoration: none;
  }

  .wordmark span,
  .wordmark i {
    color: var(--teal);
    font-style: italic;
  }

  .wordmark i {
    font-size: 15px;
  }

  .tagline {
    margin-top: var(--space-3);
    font-family: var(--font-display);
    font-size: 18px;
    font-weight: 300;
    line-height: 1.3;
  }

  .tagline span {
    color: var(--cream);
  }

  .tagline b {
    color: var(--teal);
    font-weight: inherit;
  }

  .tagline i {
    color: var(--taupe);
    font-style: normal;
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: var(--space-2) 28px;
  }

  nav a {
    color: rgb(232 250 248 / 70%);
    font-size: 15px;
    font-weight: 500;
    text-decoration: none;
    transition: color var(--ease-fast);
  }

  nav a:hover,
  nav a:focus-visible {
    color: var(--teal);
  }

  .footer-meta {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: var(--space-3);
    border-top: 1px solid var(--line-quiet);
    padding-block: 20px;
    color: rgb(232 250 248 / 52%);
    font-size: 14px;
  }

  @media (max-width: 640px) {
    .footer-main {
      grid-template-columns: 1fr;
    }

    nav {
      justify-content: flex-start;
    }
  }
</style>
